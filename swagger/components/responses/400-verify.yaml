description: Bad request
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Bad request - Something went wrong while sending verification email
