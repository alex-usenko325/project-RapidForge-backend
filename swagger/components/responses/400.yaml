description: Bad request
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Missing or invalid data
        data:
          type: object
          properties:
            message:
              type: string
              example: 'Missing or invalid request parameters.'
